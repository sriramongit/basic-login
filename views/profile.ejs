<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="stylesheets/otherStyles.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <h2 class="text-2xl">Hello <%= user.name.charAt(0).toUpperCase() + user.name.slice(1) %> 👋</h2>
    <form class="flex justify-center items-center" action="/logout/<%= user_email %>" method="POST">
        <button class="w-25 h-auto cursor-pointer mt-8 bg-red-500 px-4 py-2 rounded-md text-center" type="submit">Logout</button>
    </form>
</body>
</html>